# API

The public OpenAPI is available at `/api/openapi.json`.

- Swagger UI: `/docs`
- Redoc: `/redoc`
- Health: `/health`

Authentication is token-based; include the `Authorization` header when required.
